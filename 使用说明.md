# ClassWise 教务助手 v2.0 — 使用说明

## 一、启动方式

**双击 `start.bat`**

会弹出一个黑色窗口，自动打开默认浏览器到 ClassWise 首页。这个黑色窗口**不要关**，关了系统就停了。再用就再双击一次 `start.bat`。

> 第一次 Windows 可能提示"未知发布者"，点「更多信息」→「仍要运行」。

## 二、为什么不能直接双击 HTML

旧版（v1.0）可以双击 HTML。但 v2.0 加了 PaddleOCR 拍照识别功能，OCR 需要浏览器的 `SharedArrayBuffer` 能力，这个能力只能从 `http://localhost` 访问时才允许。所以 v2.0 改成用一个**本地小型 HTTP 服务器**（`start.bat` 启动的）来提供页面。

服务器只在你电脑上运行，**不联网，不上传任何数据**。

## 三、功能模块

| 菜单 | 作用 |
|------|------|
| 🏠 首页概览 | 看学生总数、班级数、成绩/考勤条数 |
| 👥 学生管理 | 添加、编辑、删除学生；按班级筛选 |
| 📊 成绩管理 | 按学期+考试类型录入；自动算总分平均分排名 |
| 📅 考勤记录 | 点状态按钮循环切换：出勤→缺勤→请假→迟到 |
| 📤 报表导出 | 一键导出三张 Excel：名单、成绩、考勤 |
| 🤖 AI 助手 | 调用本地 Ollama 生成评语、分析、总结、问答 |
| 📷 **OCR 录入**（新） | 拍照识别学生名单/成绩单/考勤表，自动导入 |
| ⚙️ 数据与设置 | JSON 导出导入备份、清空、载入示例数据 |

## 四、OCR + AI 双通道（新特性）

系统会根据你在「AI 助手」页选的模型，**自动选择最优识别路径**：

| 你选的 AI 模型 | OCR 路径 | 适用场景 |
|---------------|----------|---------|
| Gemma 4 E4B / 26B（多模态） | ✨ AI 直接看图 | 最准、能识别手写、能理解上下文 |
| Qwen 2.5 (0.5B / 7B) | 📷 PaddleOCR → 文字 → AI | 印刷体识别快，老电脑也能跑 |

### 使用流程

1. 进入「📷 OCR 录入」页
2. 点「上传图片」选择照片（学生名单、成绩单、考勤表照片都行）
3. 点「📷 识别图片为文字」→ 中间文本框会显示识别结果
4. **手动校对**识别结果（OCR 不是 100% 准确，老师过一眼）
5. 点最下面的三个按钮之一：
   - **📋 解析为学生名单**：AI 自动提取学号/姓名/班级，批量导入
   - **📊 解析为成绩录入**：AI 把识别结果对应到学生 + 科目分数
   - **💬 让 AI 自由处理**：自定义提问，比如"按班级分组"

## 五、AI 配置（首次使用 AI 功能必看）

### 安装 Ollama

1. 访问 https://ollama.com/download 下载安装
2. 装完后，打开命令行（cmd），按电脑配置选**一条**命令下载模型：

   | 配置 | 命令 | 说明 |
   |------|------|------|
   | 4GB 内存（老电脑） | `ollama pull qwen2.5:0.5b` | 入门，只能用文本功能 |
   | 8GB 内存（推荐） | `ollama pull qwen2.5:7b` | 中文最佳，配合 PaddleOCR 使用 |
   | 8GB 内存+想用 OCR 视觉 | `ollama pull gemma4:e4b` | **多模态，可直接看图** |
   | 16GB+ 高配 | `ollama pull gemma4` | 旗舰，能力最强 |

### 启用 OCR 视觉的关键

要让 OCR 走 ✨ AI 直接看图路线，必须用 **Gemma 4 系列**。Qwen 系列是纯文本，只能用 PaddleOCR 路线。

### 让 Ollama 接受浏览器请求

Ollama 默认会拒绝跨域请求，需要让它接受 `localhost:8765` 的请求。**用 Docker 启动时加 `-e OLLAMA_ORIGINS=*`**；直接装的话设环境变量 `OLLAMA_ORIGINS=*` 后重启 Ollama。

## 六、Docker 启动 Ollama（推荐）

如果你装了 Docker Desktop，一条命令搞定：

```bash
docker run -d -v ollama:/root/.ollama -p 11434:11434 -e OLLAMA_ORIGINS=* --name ollama ollama/ollama
docker exec -it ollama ollama pull qwen2.5:7b
```

## 七、数据备份（重要！）

数据保存在浏览器的 localStorage 里，**清浏览器缓存或换电脑会丢失**。

养成习惯：**每周点一次「⚙️ 数据与设置」→「📥 导出全部数据 (JSON)」**。

## 八、文件清单

| 文件 | 说明 |
|------|------|
| `ClassWise.html` | 主程序 |
| `start.bat` | **启动入口（双击它）** |
| `server.ps1` | 本地 HTTP 服务器（不要单独运行） |
| `使用说明.md` | 本文件 |
| `ocr/` | OCR 引擎文件夹（约 22MB，请勿删除） |

## 九、常见问题

**Q：双击 start.bat 报错"无法运行脚本"？**  
A：Windows 默认禁用 PowerShell 脚本。右键 `start.bat` → 以管理员身份运行。或参考方法二：在 PowerShell 管理员窗口里执行 `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser` 后再双击。

**Q：浏览器提示连不上 Ollama？**  
A：检查 Ollama 是否启动（任务栏小羊驼图标），并确认设置了 `OLLAMA_ORIGINS=*`。

**Q：OCR 识别失败 / 加载很慢？**  
A：首次识别要加载约 16MB 的模型，耐心等 10-30 秒。失败的话 F12 看控制台报错。

**Q：识别结果不准？**  
A：印刷体比手写体准；清晰大图比模糊小图准；先校对后再交给 AI。或换用 Gemma 4 视觉路线。

---
版本：v2.0  
更新日期：2026-05-19  
新增：OCR 录入 + AI 双通道、本地 HTTP 启动器
